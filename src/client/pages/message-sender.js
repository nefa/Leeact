/*import React from 'react';

export default class MessSend extends React.Componenet {

  render() {
    return (
      <div key={this.props.member.id}>
        {<input type="text" ref="message"/>
        <button
          onClick={evt => this.props.sendMessage({
            memberId: this.props.member.id,
            message: 'checking in... from client aka: ' + this.props.id
          })}>
                  {this.props.member.nick}
        </button>}
      </div>
    )
  }
}
*/
